# Session Log 20260427_0110

## 作業概要

panic reversal の「暴落時に買って保有し、回復条件で売る」考え方と、現在 repo に実装済みの機械ルールを `docs/strategy/` に stable path で保存する。  
合わせて今回の session log を残し、変更を push したうえで GitHub Actions の `Night Batch Self Hosted` を実際に起動する。

## 実施内容

### 1. panic reversal 戦略文書の追加

- `docs/strategy/panic-reversal-regime-recovery.md` を追加
- 通常の `RSP > 200SMA` 系順張り戦略とは別枠の **弱気 regime 専用補完戦略**として位置づけを整理
- 現行ロジックとして以下を明記
  - `RSP < 200SMA`
  - `VIX > 30`
  - `SPY RSI14 < 30`
  - `SPY < 200SMA`
  - `SPY RSI2` の 10 上抜けで entry
  - `close > SMA25 && RSI14 >= 65` で exit
  - stop loss なし

### 2. docs 用テストの追加

- `tests/strategy-docs.test.js` を追加
- strategy doc と session log の stable path / 主要記述を固定する RED -> GREEN を実施

### 3. workflow 実行対象

- GitHub Actions workflow: `Night Batch Self Hosted`
- dispatch config: `config/night_batch/bundle-foreground-reuse-config.json`

## 実行結果メモ

- commit / push: 実施前
- workflow dispatch: 実施前

## 引き継ぎ事項

- workflow 実行後は run URL と初期状態をここへ追記する
