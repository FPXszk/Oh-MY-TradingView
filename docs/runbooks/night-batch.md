# Night batch runbook

night batch の通常導線:

1. `README.md` の CLI / workflow 入口を確認する
2. `config/night_batch/` と `config/backtest/campaigns/current/` を確認する
3. 出力は `artifacts/night-batch/` と `docs/research/current/` を見る
