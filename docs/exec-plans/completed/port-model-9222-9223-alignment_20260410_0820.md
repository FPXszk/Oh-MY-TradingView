# Exec Plan: port-model-9222-9223-alignment_20260410_0820

## 目的

リポジトリの**現在の運用前提**を、`visible / 9225 attach-only` から次の前提へ修正する。

- **Windows ローカル**では TradingView Desktop を `9222` で扱う
- **WSL** からは Windows 側 CDP へ `9223` で接続する
- `9225 -> 9224` 系の現行前提は、**現在の既定運用としては誤り**として是正する

あわせて、現行コード・現行運用ドキュメント・runbook のうち、この前提と不整合な箇所を修正する。  
ただし、**historical record は書き換えない**。

## 背景

live diagnostics で次を確認済み。

- Windows ローカル `9222` は TradingView CDP に応答する
- WSL から `172.31.144.1:9223` へ到達できる
- `9225` は現在 `9224` への portproxy だが、`9224` 側が CDP を提供しておらず失敗する
- 最近の変更で repo は `9225` 前提へ寄っているため、本件は**新方針追加ではなく前提修正**である

## 既存 active plan との関係

- 直前に作成した visible 10-pattern trial plan は、`9225` 前提のため **superseded / deferred** として扱う
- 10-pattern trial 自体の再開判断は、本アラインメント完了後に行う

## 対象範囲

- CDP 接続既定値・接続ヒント・CLI help・server 説明文の修正
- launch / browser-launch / observability / health / connection の 9225 前提除去
- batch / finetune / nightly 実行系の既定ポート前提の修正
- current operational docs / runbook の既定ポート・接続例・運用説明の修正
- campaign 設定の `worker_ports` 既定値見直し
- 関連テストの更新

## 対象外

- `docs/exec-plans/completed/` 配下の completed exec-plan
- `docs/research/old/` などの archived research
- old docs / historical notes の文面修正
- `9224` / `9225` を再び既定運用として復活させる作業
- dual-worker 運用の再設計・再有効化
- blocked / deferred 中の visible 10-pattern trial の実行再開

## 変更対象ファイル

### 作成

- `docs/exec-plans/active/port-model-9222-9223-alignment_20260410_0820.md`

### 更新

#### 現行ドキュメント / runbook
- `README.md`
- `command.md`
- `docs/design-docs/dual-worker-parallel-backtest-runbook_20260406_0735.md`
- `/home/fpxszk/.copilot/session-state/4f84407c-148b-41f6-9407-bf4fea3dc382/plan.md`

#### 本番コード
- `src/connection.js`
- `src/server.js`
- `src/core/launch.js`
- `src/cli/commands/launch.js`
- `src/cli/commands/browser-launch.js`
- `src/tools/launch.js`
- `src/tools/browser-launch.js`
- `src/tools/health.js`
- `src/tools/observe.js`
- `scripts/backtest/run-finetune-bundle.mjs`
- `python/night_batch.py`

#### 設定
- `config/backtest/campaigns/external-phase1-priority-top.json`
- `config/backtest/campaigns/long-run-cross-market-100x5.json`
- `config/backtest/campaigns/long-run-jp-exit-sweep-50x3.json`
- `config/backtest/campaigns/long-run-jp-exit-sweep-100x3.json`
- `config/backtest/campaigns/long-run-us-entry-sweep-50x3.json`
- `config/backtest/campaigns/long-run-us-entry-sweep-100x3.json`
- `config/backtest/campaigns/next-long-run-us-finetune-100x10.json`
- `config/backtest/campaigns/next-long-run-jp-finetune-100x10.json`

#### テスト
- `tests/connection.test.js`
- `tests/launch.test.js`
- `tests/browser-launch.test.js`
- `tests/observability.test.js`
- `tests/campaign.test.js`
- `tests/night-batch.test.js`
- `tests/e2e.pine-loop.test.js`

### 削除

- なし

## 実装方針

1. **既定 endpoint model を修正する**
   - Windows ローカル既定: `localhost:9222`
   - WSL 利用時の既定運用: `Windows host IP:9223`
   - `9225` を既定・正本・attach-first とする説明を除去する

2. **単純な 9225 -> 9222 置換ではなく、Windows / WSL の役割差を明示する**
   - コードの既定値
   - エラーヒント
   - CLI help
   - README / command / runbook
   を一貫させる

3. **batch / campaign / nightly の既定運用を 9223 ベースへ寄せる**
   - WSL 実行系の標準例は `--host 172.31.144.1 --port(s) 9223`
   - `9223,9225` や `9225` 単独を current default として扱わない

4. **historical record は保持する**
   - completed / archived / old docs は修正しない
   - current docs 側では「現行前提」と「参考資料」を区別する

## TDD / テスト方針

### RED

先に不整合を表す失敗テストへ更新する。

- `tests/connection.test.js`
  - default / hint / endpoint 解決の期待値を 9222 / 9223 前提へ更新
- `tests/launch.test.js`
  - launch default port を 9225 から新前提へ変更
- `tests/browser-launch.test.js`
  - browser-launch default port / generated args / user-data-dir 関連期待値を更新
- `tests/observability.test.js`
  - 接続情報や hint の 9225 文言を更新
- `tests/campaign.test.js`
  - finetune bundle / campaign defaults の既定ポート期待値を更新
- `tests/night-batch.test.js`
  - preflight / default port / summary 文言を新前提へ更新
- `tests/e2e.pine-loop.test.js`
  - コメント・前提説明の不整合を修正対象として明示

### GREEN

最小限の実装でテストを通す。

- connection / launch / tool help / batch defaults / config defaults を修正
- README / command / runbook の current operational sections を新前提へ更新

### REFACTOR

重複や将来の再発要因を整理する。

- endpoint 文言・既定値の重複を定数/helper に寄せられるか確認
- 「Windows local 9222 / WSL 9223」の説明を複数箇所で同じ表現に統一
- 9225 前提の stale wording を追加で洗い出して除去

### カバレッジ方針

- 既存 repo には coverage 計測用の明示コマンドが見当たらないため、**新規ツール導入は行わない**
- その代わり、変更対象のユニット/運用系テストを追加・更新し、変更箇所を既存 test suite で実質網羅する
- 既存の coverage 手段が途中で確認できた場合のみ、その手段で **80% 以上**を確認する

## Validation commands

既存 repo のコマンド・既存スクリプトのみを使う。

### テスト

```bash
npm test
npm run test:e2e
npm run test:all
```

### 接続前提の確認

```bash
curl -sS http://127.0.0.1:9222/json/version
curl -sS http://172.31.144.1:9223/json/version
curl -sS http://172.31.144.1:9223/json/list
TV_CDP_HOST=172.31.144.1 TV_CDP_PORT=9223 node src/cli/index.js status
```

### batch / nightly 前提の確認

```bash
node scripts/backtest/run-finetune-bundle.mjs --dry-run --host 172.31.144.1 --ports 9223
python3 python/night_batch.py bundle --host 172.31.144.1 --port 9223 --dry-run
python3 python/night_batch.py nightly --host 172.31.144.1 --port 9223 --dry-run
```

## 完了条件

- repo 内の current code / current operational docs / runbook で、`9225` が current default として残っていない
- Windows local は `9222`、WSL は `9223` という説明と実装が一致している
- batch / campaign / nightly の既定運用が新前提と矛盾しない
- 既存 test suite が通る

## リスク

- `src/connection.js` が現在単一 `DEFAULT_CDP_PORT` 前提のため、Windows/WSL の差分をどう表現するかで設計判断が必要
- campaign `worker_ports` を `9223` 中心へ寄せると、過去の dual-worker 想定と衝突する可能性がある
- `9225` 参照が docs だけでなく error hint / generated message / tests に散在しているため、取りこぼしリスクがある
- docs/design-docs 配下には historical に近い文脈も混ざるため、「現行 runbook 修正」と「履歴改変」の線引きに注意が必要
- WSL 側 host 解決は環境差があるため、`9223` だけでなく `TV_CDP_HOST` 指定ルールも明示しないと再混乱の可能性がある

## 実装ステップ

- [ ] `README.md`、`command.md`、runbook を見直し、current operational wording の 9225 前提箇所を特定する
- [ ] `src/connection.js` の既定 endpoint 解決方針を、`9225` 前提から `Windows local 9222 / WSL 9223` 前提へ更新する
- [ ] `src/server.js`、CLI command help、tool description、health/observe hint の 9225 文言を修正する
- [ ] `scripts/backtest/run-finetune-bundle.mjs` と `python/night_batch.py` の既定ポート前提を 9223 ベースへ修正する
- [ ] `config/backtest/campaigns/*.json` の `worker_ports` を current operating assumption に合わせて見直す
- [ ] 関連テストを RED から更新し、最小実装で GREEN にする
- [ ] endpoint 文言・既定値の重複を整理し、REFACTOR を行う
- [ ] `npm test`、`npm run test:e2e`、`npm run test:all` を実行して回帰確認する
- [ ] `curl` / `tv status` / batch dry-run / nightly dry-run で `9222` / `9223` 前提の運用例を検証する
- [ ] historical record を除外したうえで、取りこぼした `9225` current-default wording がないか最終検索する

## レビュー観点

- 「Windows local 9222 / WSL 9223」が**コード・テスト・ドキュメントで同じ意味**になっているか
- 9225 が current default / attach-first / visible-first の正本として残っていないか
- dual-worker や 9224/9225 を historical reference として残す場合でも、current operational guidance と混線していないか
- 修正が historical record 改変に踏み込んでいないか
